---
# role: environment
# file: defaults/main.yml

# String values for named assignments in /etc/environment; other names are preserved.
# Values use the portable PAM/systemd subset documented in the README.
environment_vars: {}
# http_proxy: "http://proxy.example.org:3128"
# no_proxy: "127.0.0.1,localhost"

# Complete environment.d files with unique .conf names and string-valued entries.
environment_d_files: []
# - name: "90-application.conf"
#   entries:
#     APPLICATION_LABEL: 'Example "application"'
#     APPLICATION_PATH: '/opt/application/bin:$PATH'
